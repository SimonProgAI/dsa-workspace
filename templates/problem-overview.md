# <Problem Name>

<!--
Copy this file into a problem folder as problem-overview.md and fill it in
BEFORE you start coding. Writing the summary in your own words is the point of
the file; the rest is there so you can re-read a problem months later and get
your own reasoning back instead of re-deriving it.

Delete any heading that genuinely does not apply.
-->

## Summary

One or two sentences, in your own words. Not the original problem statement —
your restatement of what is actually being asked.

## Constraints

- Input sizes and value ranges (e.g. `1 <= nums.length <= 10^5`, `-10^9 <= nums[i] <= 10^9`).
- Guarantees you may rely on (sorted? distinct? non-empty? exactly one answer?).
- Anything the problem does *not* promise, which is where the edge cases come from.

## Examples

| Input | Output | Why |
| --- | --- | --- |
| `...` | `...` | ... |
| `...` | `...` | ... |

## Edge Cases

- Empty input
- Single element
- All elements equal / duplicates
- Negative numbers, zero
- Value ranges that overflow a 32-bit int
- The "no answer exists" case

Keep this list in sync with the `edgeCases` array in `tests/<problem>.json`.

## Complexity

- **Time:** `O(?)` — because ...
- **Space:** `O(?)` — because ...
- Brute force for comparison: `O(?)` time, `O(?)` space.

## Why This Category

What makes this a <category> problem rather than something else? Which cue in
the problem statement should make you reach for this technique next time?

## Thought Process

- First idea, and why it was too slow or too complicated.
- The observation that unlocked the real approach.
- What you got wrong on the first attempt.
- Related problems this generalises to.

## Links

- LeetCode: <url>
- GeeksforGeeks: <url>
- AlgoMonster / NeetCode: <url>
